/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"], // Tailwind scans all files in src
  theme: { extend: {} }, // default theme, you can customize later
  plugins: [], // no plugins for now
};
